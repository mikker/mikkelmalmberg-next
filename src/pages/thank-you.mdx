import ArticleLayout from "../components/ArticleLayout";

# Thank you!

Hopefully I'll pick my ass up and get something written and sent soon. Either way, so glad to have you on the list &hearts;

export default ({ children, ogImage }) => (
  <ArticleLayout title="/thank-you">
    <div className="text-center max-w-prose mx-auto">{children}</div>
  </ArticleLayout>
);
